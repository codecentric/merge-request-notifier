export interface NotificationOptions {
    title: string
    subtitle: string
    body: string

    // silent: boolean
    // icon?: (string) | (NativeImage)
    // hasReply?: boolean
    // sound?: string
    // closeButtonText?: string;
}
